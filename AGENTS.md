# Project Rules

- NEVER run `npm run build`, `tsc`, `vite build`, or any build command unless explicitly asked.
- NEVER run `npm run dev`, `vite`, `tsx watch`, or any dev server command unless explicitly asked.
- If the user asks to "test" or "check" code, only report what you changed — do NOT run build or dev.
